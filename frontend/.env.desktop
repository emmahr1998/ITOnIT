# Non-secret build configuration for the Electron desktop shell (see
# desktop/). Loaded only by `vite --mode desktop` / `vite build --mode desktop`.
VITE_APP_MODE=desktop
VITE_API_BASE_URL=http://localhost:8000
